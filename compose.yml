services:
  simuphys:
    image: ${SIMUPHYS_IMAGE:-registry.mmyumu.fr/simuphys:1.3.1}
    build:
      context: .
    container_name: simuphys
    ports:
      - "127.0.0.1:3000:3000"
    restart: unless-stopped
